[project]
name = "tested-voting-collector"
authors = ["Stacks Developer"]
description = "A decentralized voting collector and management system"
telemetry = false
requirements = []
[contracts.voting-collector]
path = "contracts/voting-collector.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
